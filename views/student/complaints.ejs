<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> - Mess Management System</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
  <script>
    tailwind.config = {
      theme: {
        extend: {
          animation: {
            'float': 'float 6s ease-in-out infinite',
            'pulse-slow': 'pulse 3s cubic-bezier(0.4, 0, 0.6, 1) infinite',
          },
          keyframes: {
            float: {
              '0%, 100%': { transform: 'translateY(0px)' },
              '50%': { transform: 'translateY(-10px)' },
            }
          }
        }
      }
    }
  </script>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-image: linear-gradient(rgba(0, 0, 0, 0.4), rgba(0, 0, 0, 0.4)), url('https://images.unsplash.com/photo-1512058564366-18560ea541a6?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=1470&q=80');
      background-size: cover;
      background-position: center;
      background-attachment: fixed;
      min-height: 100vh;
    }
    .glass-morphism {
      background: rgba(255, 255, 255, 0.1);
      backdrop-filter: blur(10px);
      border: 1px solid rgba(255, 255, 255, 0.2);
    }
    .btn-primary {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      transition: all 0.3s ease;
    }
    .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 10px 20px rgba(102, 126, 234, 0.3);
    }
    .form-input {
      border: 2px solid rgba(255, 255, 255, 0.3);
      background: rgba(255, 255, 255, 0.1);
      transition: all 0.3s ease;
      color: white;
    }
    .form-input:focus {
      border-color: #667eea;
      box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.3);
      background: rgba(255, 255, 255, 0.2);
    }
    .form-input::placeholder {
      color: rgba(255, 255, 255, 0.7);
    }
  </style>
</head>
<body>
  <%- include('../_header') %>
  <main class="container mx-auto px-6 py-12">
    <div class="mb-8">
      <h1 class="text-5xl font-bold text-white mb-2 animate-pulse-slow">Complaints</h1>
      <p class="text-purple-200 text-lg">Report issues and track their resolution</p>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
      <div class="lg:col-span-2">
        <div class="glass-morphism rounded-xl p-8">
          <h2 class="text-2xl font-bold text-white mb-6 flex items-center gap-3">
            <i class="fas fa-exclamation-circle text-red-400"></i> File a Complaint
          </h2>
          
          <form method="POST" action="/student/complaints" class="space-y-6">
            <div>
              <label class="block text-white font-bold mb-2" for="subject">
                <i class="fas fa-heading mr-2"></i>Subject
              </label>
              <input type="text" id="subject" name="subject" placeholder="Brief description of the issue"
                     class="form-input w-full px-4 py-3 rounded-lg" required>
            </div>

            <div>
              <label class="block text-white font-bold mb-2" for="message">
                <i class="fas fa-comment-alt mr-2"></i>Detailed Message
              </label>
              <textarea id="message" name="message" rows="5" placeholder="Please provide detailed information about your complaint..."
                        class="form-input w-full px-4 py-3 rounded-lg resize-none" required></textarea>
            </div>

            <button type="submit" class="btn-primary w-full text-white px-6 py-3 rounded-lg hover:shadow-xl font-bold text-lg transition hover:scale-105 transform">
              <i class="fas fa-paper-plane mr-2"></i> Submit Complaint
            </button>
          </form>
        </div>

        <div class="glass-morphism rounded-xl p-8 mt-8">
          <h3 class="text-2xl font-bold text-white mb-6 flex items-center gap-3">
            <i class="fas fa-history text-blue-400"></i> Your Complaints
          </h3>
          
          <% if (complaints.length === 0) { %>
            <div class="text-center py-8">
              <div class="w-16 h-16 bg-white bg-opacity-20 rounded-lg flex items-center justify-center mx-auto mb-4">
                <i class="fas fa-inbox text-white text-2xl"></i>
              </div>
              <p class="text-purple-200">No complaints filed yet</p>
            </div>
          <% } else { %>
            <div class="space-y-4">
              <% complaints.forEach(complaint => { %>
                <div class="bg-white bg-opacity-10 rounded-lg p-4 hover:scale-105 transform transition-all duration-300">
                  <div class="flex justify-between items-start mb-2">
                    <h4 class="text-white font-bold"><%= complaint.subject %></h4>
                    <span class="text-xs px-2 py-1 rounded-full <%= complaint.status === 'resolved' ? 'bg-green-400 text-white' : complaint.status === 'in-progress' ? 'bg-yellow-400 text-white' : 'bg-red-400 text-white' %>">
                      <%= complaint.status || 'pending' %>
                    </span>
                  </div>
                  <p class="text-purple-200 text-sm mb-2"><%= complaint.message %></p>
                  <p class="text-purple-300 text-xs">Filed on: <%= new Date(complaint.createdAt).toLocaleDateString() %></p>
                </div>
              <% }) %>
            </div>
          <% } %>
        </div>
      </div>

      <div>
        <div class="glass-morphism rounded-xl p-8">
          <h3 class="text-xl font-bold text-white mb-6 flex items-center gap-3">
            <i class="fas fa-info-circle text-blue-400"></i> Complaint Guidelines
          </h3>
          <div class="space-y-4">
            <div class="bg-white bg-opacity-10 rounded-lg p-4">
              <h4 class="text-white font-bold mb-2">Be Specific</h4>
              <p class="text-purple-200 text-sm">Provide clear details about the issue</p>
            </div>
            
            <div class="bg-white bg-opacity-10 rounded-lg p-4">
              <h4 class="text-white font-bold mb-2">Be Respectful</h4>
              <p class="text-purple-200 text-sm">Use polite language when describing problems</p>
            </div>
            
            <div class="bg-white bg-opacity-10 rounded-lg p-4">
              <h4 class="text-white font-bold mb-2">Response Time</h4>
              <p class="text-purple-200 text-sm">Usually resolved within 24-48 hours</p>
            </div>
          </div>
        </div>

        <div class="glass-morphism rounded-xl p-8 mt-8">
          <h3 class="text-xl font-bold text-white mb-6 flex items-center gap-3">
            <i class="fas fa-phone text-green-400"></i> Quick Contact
          </h3>
          <div class="space-y-3">
            <div class="flex items-center gap-3">
              <i class="fas fa-headset text-purple-200"></i>
              <span class="text-purple-200">Mess Helpline: 1800-123-456</span>
            </div>
            <div class="flex items-center gap-3">
              <i class="fas fa-envelope text-purple-200"></i>
              <span class="text-purple-200">mess@college.edu</span>
            </div>
            <div class="flex items-center gap-3">
              <i class="fas fa-clock text-purple-200"></i>
              <span class="text-purple-200">24/7 Support Available</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </main>
  <%- include('../_footer') %>
</body>
</html>
