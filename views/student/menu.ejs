<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> - Mess Management System</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #2d1b1b;
      background-image: 
        linear-gradient(rgba(0, 0, 0, 0.8), rgba(0, 0, 0, 0.7)),
        url('https://images.unsplash.com/photo-1512058564366-18560ea541a6?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=1470&q=80');
      background-size: cover;
      background-position: center;
      background-attachment: fixed;
      min-height: 100vh;
      position: relative;
      background-repeat: no-repeat;
    }
    .glass-morphism {
      background: rgba(255, 255, 255, 0.1);
      backdrop-filter: blur(15px);
      border: 1px solid rgba(255, 255, 255, 0.2);
      border-radius: 16px;
      box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
    }
  </style>
</head>
<body>
  <%- include('../_header') %>
  <main class="container mx-auto px-6 py-12">
    <div class="mb-8">
      <h1 class="text-5xl font-bold text-white mb-2 animate-pulse-slow">Mess Menu</h1>
      <p class="text-purple-200 text-lg">View today's meal plans and menu schedules</p>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
      <div class="lg:col-span-2">
        <div class="glass-morphism rounded-xl p-8">
          <h2 class="text-2xl font-bold text-white mb-6 flex items-center gap-3">
            <i class="fas fa-utensils text-orange-400"></i> Today's Menu
          </h2>
          <div class="space-y-4">
            <% if (menu && menu.breakfast && menu.breakfast.length > 0) { %>
              <div class="bg-gradient-to-r from-yellow-400 to-orange-400 rounded-lg p-4 hover:scale-105 transform transition-all duration-300">
                <p class="font-bold text-white">🌅 Breakfast</p>
                <p class="text-sm text-white mt-1"><%= menu.breakfast.join(', ') %></p>
              </div>
            <% } %>
            
            <% if (menu && menu.lunch && menu.lunch.length > 0) { %>
              <div class="bg-gradient-to-r from-blue-400 to-cyan-400 rounded-lg p-4 hover:scale-105 transform transition-all duration-300">
                <p class="font-bold text-white">🍽️ Lunch</p>
                <p class="text-sm text-white mt-1"><%= menu.lunch.join(', ') %></p>
              </div>
            <% } %>
            
            <% if (menu && menu.dinner && menu.dinner.length > 0) { %>
              <div class="bg-gradient-to-r from-purple-400 to-pink-400 rounded-lg p-4 hover:scale-105 transform transition-all duration-300">
                <p class="font-bold text-white">🌙 Dinner</p>
                <p class="text-sm text-white mt-1"><%= menu.dinner.join(', ') %></p>
              </div>
            <% } %>
            
            <% if (!menu || (menu.breakfast.length === 0 && menu.lunch.length === 0 && menu.dinner.length === 0)) { %>
              <div class="bg-gradient-to-r from-gray-400 to-gray-600 rounded-lg p-4">
                <p class="font-bold text-white">No Menu Available</p>
                <p class="text-sm text-white mt-1">Menu has not been set for today. Please check back later.</p>
              </div>
            <% } %>
          </div>
        </div>
      </div>

      <div>
        <div class="glass-morphism rounded-xl p-8">
          <h3 class="text-xl font-bold text-white mb-4 flex items-center gap-3">
            <i class="fas fa-clock text-blue-400"></i> Meal Timings
          </h3>
          <div class="space-y-3">
            <div class="flex justify-between items-center">
              <span class="text-purple-200">Breakfast</span>
              <span class="text-white font-bold">7:00 AM - 9:00 AM</span>
            </div>
            <div class="flex justify-between items-center">
              <span class="text-purple-200">Lunch</span>
              <span class="text-white font-bold">12:00 PM - 2:00 PM</span>
            </div>
            <div class="flex justify-between items-center">
              <span class="text-purple-200">Dinner</span>
              <span class="text-white font-bold">7:00 PM - 9:00 PM</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </main>
  <%- include('../_footer') %>
</body>
</html>
