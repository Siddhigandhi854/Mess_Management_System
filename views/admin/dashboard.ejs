<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> - Mess Management System</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
  <script>
    tailwind.config = {
      theme: {
        extend: {
          animation: {
            'float': 'float 6s ease-in-out infinite',
            'pulse-slow': 'pulse 3s cubic-bezier(0.4, 0, 0.6, 1) infinite',
            'bounce-slow': 'bounce 2s infinite',
            'glow': 'glow 2s ease-in-out infinite',
          },
          keyframes: {
            float: {
              '0%, 100%': { transform: 'translateY(0px)' },
              '50%': { transform: 'translateY(-10px)' },
            },
            bounce: {
              '0%, 100%': { transform: 'translateY(0)' },
              '50%': { transform: 'translateY(-20px)' },
            },
            glow: {
              '0%, 100%': { boxShadow: '0 0 20px rgba(239, 68, 68, 0.3)' },
              '50%': { boxShadow: '0 0 40px rgba(239, 68, 68, 0.6)' },
            }
          }
        }
      }
    }
  </script>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #2d1b1b;
      background-image: 
        linear-gradient(rgba(0, 0, 0, 0.8), rgba(0, 0, 0, 0.7)),
        url('https://images.pexels.com/photos/3184418/pexels-photo-3184418.jpeg?auto=compress&cs=tinysrgb&w=1260&h=750&dpr=1');
      background-size: cover;
      background-position: center;
      background-attachment: fixed;
      min-height: 100vh;
      position: relative;
      background-repeat: no-repeat;
    }
    body::before {
      content: '';
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: linear-gradient(135deg, 
        rgba(220, 38, 38, 0.15) 0%, 
        rgba(239, 68, 68, 0.1) 50%, 
        rgba(251, 146, 60, 0.1) 100%);
      pointer-events: none;
      z-index: 1;
    }
    .container {
      position: relative;
      z-index: 2;
    }
    .glass-morphism {
      background: rgba(255, 255, 255, 0.1);
      backdrop-filter: blur(10px);
      border: 1px solid rgba(255, 255, 255, 0.2);
      box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
    }
    .glass-morphism:hover {
      box-shadow: 0 12px 40px rgba(0, 0, 0, 0.15);
      transform: translateY(-2px);
    }
    .btn-primary {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      transition: all 0.3s ease;
    }
    .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 10px 20px rgba(102, 126, 234, 0.3);
    }
    .btn-success {
      background: linear-gradient(135deg, #10b981 0%, #059669 100%);
      transition: all 0.3s ease;
    }
    .btn-success:hover {
      transform: translateY(-2px);
      box-shadow: 0 10px 20px rgba(16, 185, 129, 0.3);
    }
    .btn-danger {
      background: linear-gradient(135deg, #ef4444 0%, #dc2626 100%);
      transition: all 0.3s ease;
    }
    .btn-danger:hover {
      transform: translateY(-2px);
      box-shadow: 0 10px 20px rgba(239, 68, 68, 0.3);
    }
    .form-input {
      border: 2px solid rgba(255, 255, 255, 0.3);
      background: rgba(255, 255, 255, 0.1);
      transition: all 0.3s ease;
      color: white;
    }
    .form-input:focus {
      border-color: #667eea;
      box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.3);
      background: rgba(255, 255, 255, 0.2);
    }
    .form-input::placeholder {
      color: rgba(255, 255, 255, 0.7);
    }
    .admin-accent {
      background: linear-gradient(135deg, #dc2626 0%, #ef4444 100%);
    }
    .admin-accent-hover:hover {
      background: linear-gradient(135deg, #b91c1c 0%, #dc2626 100%);
    }
    .danger-border {
      border-left: 4px solid #ef4444;
    }
    .success-border {
      border-left: 4px solid #10b981;
    }
    .warning-border {
      border-left: 4px solid #f59e0b;
    }
    .info-border {
      border-left: 4px solid #3b82f6;
    }
    .stat-card {
      background: rgba(255, 255, 255, 0.1);
      backdrop-filter: blur(10px);
      border: 1px solid rgba(255, 255, 255, 0.2);
      transition: all 0.3s ease;
    }
    .stat-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
    }
    .feature-card {
      background: rgba(255, 255, 255, 0.1);
      backdrop-filter: blur(10px);
      border: 1px solid rgba(255, 255, 255, 0.2);
      transition: all 0.3s ease;
    }
    .feature-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
    }
    .hover-lift {
      transition: all 0.3s ease;
    }
    .hover-lift:hover {
      transform: translateY(-8px);
      box-shadow: 0 25px 50px rgba(0, 0, 0, 0.2);
    }
    .text-shadow {
      text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.1);
    }
    .glow-text {
      text-shadow: 0 0 20px rgba(255, 255, 255, 0.3);
    }
    .pulse-animation {
      animation: pulse-slow 3s cubic-bezier(0.4, 0, 0.6, 1) infinite;
    }
    .float-animation {
      animation: float 6s ease-in-out infinite;
    }
    .bounce-animation {
      animation: bounce-slow 2s infinite;
    }
    .glow-animation {
      animation: glow 2s ease-in-out infinite;
    }
    .animate-float {
      animation: float 6s ease-in-out infinite;
    }
    @keyframes float {
      0%, 100% { transform: translateY(0px); }
      50% { transform: translateY(-15px); }
    }
    .icon-pulse {
      animation: iconPulse 2s infinite;
    }
    @keyframes iconPulse {
      0% { transform: scale(1); }
      50% { transform: scale(1.1); }
      100% { transform: scale(1); }
    }
    .text-glow {
      text-shadow: 0 0 20px rgba(255, 255, 255, 0.5);
    }
  </style>
</head>
<body>
  <%- include('../_header') %>
  <main class="container mx-auto px-6 py-12">
    <div class="mb-8 text-center">
      <div class="inline-block mb-4">
        <div class="w-24 h-24 bg-gradient-to-br from-red-400 to-orange-400 rounded-full flex items-center justify-center animate-float icon-pulse">
          <i class="fas fa-shield-alt text-white text-4xl"></i>
        </div>
      </div>
      <h1 class="text-5xl font-bold text-white mb-2 animate-pulse-slow text-glow">Welcome, <span class="text-transparent bg-clip-text bg-gradient-to-r from-red-400 to-orange-400"><%= currentUser.name %></span>!</h1>
      <p class="text-purple-200 text-lg">Administrator Dashboard - Complete System Control</p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-4 gap-6 mb-8">
      <div class="stat-card rounded-xl p-6 danger-border hover-lift">
        <div class="flex items-center justify-between">
          <div>
            <p class="text-purple-200 font-semibold">Total Students</p>
            <p class="text-3xl font-bold text-white mt-2"><%= students %></p>
          </div>
          <div class="w-14 h-14 bg-red-400 rounded-full flex items-center justify-center animate-pulse">
            <i class="fas fa-users text-white text-2xl"></i>
          </div>
        </div>
      </div>

      <div class="stat-card rounded-xl p-6 warning-border hover-lift">
        <div class="flex items-center justify-between">
          <div>
            <p class="text-purple-200 font-semibold">Unpaid Bills</p>
            <p class="text-3xl font-bold text-white mt-2"><%= unpaidCount %></p>
          </div>
          <div class="w-14 h-14 bg-orange-400 rounded-full flex items-center justify-center animate-pulse">
            <i class="fas fa-exclamation-triangle text-white text-2xl"></i>
          </div>
        </div>
      </div>

      <div class="stat-card rounded-xl p-6 success-border hover-lift">
        <div class="flex items-center justify-between">
          <div>
            <p class="text-purple-200 font-semibold">Monthly Revenue</p>
            <p class="text-3xl font-bold text-green-300 mt-2">₹<%= (unpaidCount * 3000).toLocaleString() %></p>
          </div>
          <div class="w-14 h-14 bg-green-400 rounded-full flex items-center justify-center animate-pulse">
            <i class="fas fa-chart-line text-white text-2xl"></i>
          </div>
        </div>
      </div>

      <div class="stat-card rounded-xl p-6 info-border hover-lift">
        <div class="flex items-center justify-between">
          <div>
            <p class="text-purple-200 font-semibold">System Health</p>
            <p class="text-3xl font-bold text-white mt-2">98%</p>
          </div>
          <div class="w-14 h-14 bg-blue-400 rounded-full flex items-center justify-center animate-pulse">
            <i class="fas fa-heartbeat text-white text-2xl"></i>
          </div>
        </div>
      </div>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
      <div class="lg:col-span-2">
        <div class="glass-morphism rounded-xl p-8">
          <h2 class="text-2xl font-bold text-white mb-6 flex items-center gap-3">
            <i class="fas fa-shield-alt text-red-400"></i> Administrative Controls
          </h2>
          <div class="grid grid-cols-2 md:grid-cols-3 gap-4">
            <a href="/admin/users" class="admin-accent text-white px-4 py-3 rounded-lg hover:shadow-xl font-bold text-lg transition hover:scale-105 transform block text-center">
              <i class="fas fa-users-cog"></i> Users
            </a>
            <a href="/admin/payments" class="admin-accent text-white px-4 py-3 rounded-lg hover:shadow-xl font-bold text-lg transition hover:scale-105 transform block text-center">
              <i class="fas fa-file-invoice-dollar"></i> Billing
            </a>
            <a href="/admin/food-config" class="admin-accent text-white px-4 py-3 rounded-lg hover:shadow-xl font-bold text-lg transition hover:scale-105 transform block text-center">
              <i class="fas fa-utensils"></i> Food Config
            </a>
            <a href="/admin/reports" class="admin-accent text-white px-4 py-3 rounded-lg hover:shadow-xl font-bold text-lg transition hover:scale-105 transform block text-center">
              <i class="fas fa-tasks"></i> Reports
            </a>
            <a href="/admin/settings" class="admin-accent text-white px-4 py-3 rounded-lg hover:shadow-xl font-bold text-lg transition hover:scale-105 transform block text-center">
              <i class="fas fa-cogs"></i> Settings
            </a>
            <a href="/admin/database" class="admin-accent text-white px-4 py-3 rounded-lg hover:shadow-xl font-bold text-lg transition hover:scale-105 transform block text-center">
              <i class="fas fa-database"></i> Database
            </a>
          </div>
        </div>

        <div class="glass-morphism rounded-xl p-8 mt-8">
          <h3 class="text-2xl font-bold text-white mb-6 flex items-center gap-3">
            <i class="fas fa-history text-blue-400"></i> System Logs
          </h3>
          <div class="space-y-3 text-sm">
            <div class="flex items-start gap-4 pb-4 border-b border-purple-300">
              <div class="w-10 h-10 bg-blue-400 rounded-full flex items-center justify-center flex-shrink-0">
                <i class="fas fa-user-plus text-white"></i>
              </div>
              <div>
                <p class="font-bold text-white">New User Registration</p>
                <p class="text-purple-200">Priya Singh registered as Student - Jan 31, 2:15 PM</p>
              </div>
            </div>
            <div class="flex items-start gap-4 pb-4 border-b border-purple-300">
              <div class="w-10 h-10 bg-green-400 rounded-full flex items-center justify-center flex-shrink-0">
                <i class="fas fa-wallet text-white"></i>
              </div>
              <div>
                <p class="font-bold text-white">Payment Processed</p>
                <p class="text-purple-200">₹<%= (unpaidCount * 3000).toLocaleString() %> pending collection from <%= unpaidCount %> students</p>
              </div>
            </div>
            <div class="flex items-start gap-4 pb-4 border-b border-purple-300">
              <div class="w-10 h-10 bg-orange-400 rounded-full flex items-center justify-center flex-shrink-0">
                <i class="fas fa-exclamation-triangle text-white"></i>
              </div>
              <div>
                <p class="font-bold text-white">System Alert</p>
                <p class="text-purple-200">Database backup completed - Jan 31, 12:00 AM</p>
              </div>
            </div>
          </div>
        </div>
      </div>

      <div>
        <div class="glass-morphism rounded-xl p-8">
          <h3 class="text-xl font-bold text-white mb-6 flex items-center gap-3">
            <i class="fas fa-chart-line text-green-400"></i> Revenue Overview
          </h3>
          <div class="space-y-4">
            <div class="bg-gradient-to-r from-green-400 to-emerald-400 rounded-lg p-4 hover:scale-105 transform transition-all duration-300">
              <p class="text-sm text-white">This Month</p>
              <p class="text-2xl font-bold text-white mt-1">₹<%= ((students - unpaidCount) * 3000).toLocaleString() %></p>
              <p class="text-xs text-white mt-2"><i class="fas fa-check"></i> <%= students - unpaidCount %> students paid</p>
            </div>
            <div class="bg-gradient-to-r from-orange-400 to-yellow-400 rounded-lg p-4 hover:scale-105 transform transition-all duration-300">
              <p class="text-sm text-white">Pending Collection</p>
              <p class="text-2xl font-bold text-white mt-1">₹<%= (unpaidCount * 3000).toLocaleString() %></p>
              <p class="text-xs text-white mt-2"><i class="fas fa-clock"></i> From <%= unpaidCount %> students</p>
            </div>
          </div>
        </div>

        <div class="glass-morphism rounded-xl p-8 mt-8">
          <h3 class="text-xl font-bold text-white mb-4 flex items-center gap-3">
            <i class="fas fa-tachometer-alt text-purple-400"></i> Key Metrics
          </h3>
          <div class="space-y-3">
            <div>
              <div class="flex justify-between items-center mb-2">
                <span class="text-purple-200">Server Uptime</span>
                <span class="text-white font-bold">99.9%</span>
              </div>
              <div class="w-full bg-purple-200 rounded-full h-2">
                <div class="bg-green-400 h-2 rounded-full" style="width: 99.9%"></div>
              </div>
            </div>
            <div>
              <div class="flex justify-between items-center mb-2">
                <span class="text-purple-200">Storage Used</span>
                <span class="text-white font-bold">45%</span>
              </div>
              <div class="w-full bg-purple-200 rounded-full h-2">
                <div class="bg-blue-400 h-2 rounded-full" style="width: 45%"></div>
              </div>
            </div>
            <div>
              <div class="flex justify-between items-center mb-2">
                <span class="text-purple-200">Active Users</span>
                <span class="text-white font-bold">87%</span>
              </div>
              <div class="w-full bg-purple-200 rounded-full h-2">
                <div class="bg-orange-400 h-2 rounded-full" style="width: 87%"></div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </main>
  <%- include('../_footer') %>
</body>
</html>
