<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> - Mess Management System</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
  <script>
    tailwind.config = {
      theme: {
        extend: {
          animation: {
            'float': 'float 6s ease-in-out infinite',
            'pulse-slow': 'pulse 3s cubic-bezier(0.4, 0, 0.6, 1) infinite',
          },
          keyframes: {
            float: {
              '0%, 100%': { transform: 'translateY(0px)' },
              '50%': { transform: 'translateY(-10px)' },
            }
          }
        }
      }
    }
  </script>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-image: linear-gradient(rgba(0, 0, 0, 0.4), rgba(0, 0, 0, 0.4)), url('https://images.unsplash.com/photo-1512058564366-18560ea541a6?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=1470&q=80');
      background-size: cover;
      background-position: center;
      background-attachment: fixed;
      min-height: 100vh;
    }
    .glass-morphism {
      background: rgba(255, 255, 255, 0.1);
      backdrop-filter: blur(10px);
      border: 1px solid rgba(255, 255, 255, 0.2);
    }
    .btn-primary {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      transition: all 0.3s ease;
    }
    .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 10px 20px rgba(102, 126, 234, 0.3);
    }
    .form-input {
      border: 2px solid rgba(255, 255, 255, 0.3);
      background: rgba(255, 255, 255, 0.1);
      transition: all 0.3s ease;
      color: white;
    }
    .form-input:focus {
      border-color: #667eea;
      box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.3);
      background: rgba(255, 255, 255, 0.2);
    }
  </style>
</head>
<body>
  <%- include('../_header') %>
  <main class="container mx-auto px-6 py-12">
    <div class="mb-8">
      <h1 class="text-5xl font-bold text-white mb-2 animate-pulse-slow">Manage Menu</h1>
      <p class="text-purple-200 text-lg">Create and manage weekly meal plans</p>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
      <div class="lg:col-span-2">
        <div class="glass-morphism rounded-xl p-8">
          <h2 class="text-2xl font-bold text-white mb-6 flex items-center gap-3">
            <i class="fas fa-plus-circle text-green-400"></i> Add Menu Item
          </h2>
          
          <form method="POST" action="/mess/menu" class="space-y-6">
            <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
              <div>
                <label class="block text-white font-bold mb-2" for="date">
                  <i class="fas fa-calendar mr-2"></i>Date
                </label>
                <input type="date" id="date" name="date" 
                       class="form-input px-4 py-3 rounded-lg">
              </div>
              
              <div>
                <label class="block text-white font-bold mb-2" for="weekday">
                  <i class="fas fa-calendar-day mr-2"></i>Weekday
                </label>
                <select id="weekday" name="weekday" class="form-input px-4 py-3 rounded-lg">
                  <option value="">Select Weekday</option>
                  <option value="Monday">Monday</option>
                  <option value="Tuesday">Tuesday</option>
                  <option value="Wednesday">Wednesday</option>
                  <option value="Thursday">Thursday</option>
                  <option value="Friday">Friday</option>
                  <option value="Saturday">Saturday</option>
                  <option value="Sunday">Sunday</option>
                </select>
              </div>
            </div>

            <div>
              <label class="block text-white font-bold mb-2" for="mealType">
                <i class="fas fa-utensils mr-2"></i>Meal Type
              </label>
              <select id="mealType" name="mealType" class="form-input px-4 py-3 rounded-lg">
                <option value="">Select Meal Type</option>
                <option value="breakfast">Breakfast</option>
                <option value="lunch">Lunch</option>
                <option value="dinner">Dinner</option>
              </select>
            </div>

            <div>
              <label class="block text-white font-bold mb-2" for="items">
                <i class="fas fa-list mr-2"></i>Menu Items
              </label>
              <textarea id="items" name="items" rows="3" 
                        placeholder="Enter items separated by commas (e.g., Rice, Dal, Roti, Sabzi)"
                        class="form-input w-full px-4 py-3 rounded-lg resize-none" required></textarea>
              <p class="text-purple-200 text-sm mt-2">Separate multiple items with commas</p>
            </div>

            <button type="submit" class="btn-primary w-full text-white px-6 py-3 rounded-lg hover:shadow-xl font-bold text-lg transition hover:scale-105 transform">
              <i class="fas fa-plus mr-2"></i> Add Menu Item
            </button>
          </form>
        </div>

        <div class="glass-morphism rounded-xl p-8 mt-8">
          <h3 class="text-2xl font-bold text-white mb-6 flex items-center gap-3">
            <i class="fas fa-list text-blue-400"></i> Current Menu Items
          </h3>
          
          <% if (menus.length === 0) { %>
            <div class="text-center py-12">
              <div class="w-16 h-16 bg-white bg-opacity-20 rounded-lg flex items-center justify-center mx-auto mb-4">
                <i class="fas fa-utensils text-white text-2xl"></i>
              </div>
              <p class="text-purple-200 text-lg">No menu items added yet</p>
            </div>
          <% } else { %>
            <div class="space-y-4">
              <% menus.forEach((menu, index) => { %>
                <div class="bg-white bg-opacity-10 rounded-lg p-4 hover:scale-105 transform transition-all duration-300">
                  <div class="flex justify-between items-start mb-2">
                    <div>
                      <h4 class="text-white font-bold text-lg">
                        <%= menu.weekday || 'Custom Date' %> - <%= menu.mealType %>
                      </h4>
                      <p class="text-purple-200 text-sm"><%= menu.date ? new Date(menu.date).toLocaleDateString() : '' %></p>
                    </div>
                    <form method="GET" action="/mess/menu/<%= menu._id %>/delete" class="inline">
                      <button type="submit" class="bg-red-400 text-white px-3 py-1 rounded text-sm hover:bg-red-500 transition hover:scale-105 transform">
                        <i class="fas fa-trash"></i> Delete
                      </button>
                    </form>
                  </div>
                  
                  <div class="flex flex-wrap gap-2">
                    <% menu.items.forEach(item => { %>
                      <span class="bg-white bg-opacity-20 px-3 py-1 rounded-full text-white text-sm">
                        <%= item.name %>
                      </span>
                    <% }) %>
                  </div>
                </div>
              <% }) %>
            </div>
          <% } %>
        </div>
      </div>

      <div>
        <div class="glass-morphism rounded-xl p-8">
          <h3 class="text-xl font-bold text-white mb-6 flex items-center gap-3">
            <i class="fas fa-star text-yellow-400"></i> Menu Planning Tips
          </h3>
          <div class="space-y-4">
            <div class="bg-white bg-opacity-10 rounded-lg p-4">
              <h4 class="text-white font-bold mb-2">Nutritional Balance</h4>
              <p class="text-purple-200 text-sm">Include proteins, carbs, and vegetables in each meal</p>
            </div>
            
            <div class="bg-white bg-opacity-10 rounded-lg p-4">
              <h4 class="text-white font-bold mb-2">Variety is Key</h4>
              <p class="text-purple-200 text-sm">Change menu items weekly to keep students interested</p>
            </div>
            
            <div class="bg-white bg-opacity-10 rounded-lg p-4">
              <h4 class="text-white font-bold mb-2">Student Preferences</h4>
              <p class="text-purple-200 text-sm">Consider student feedback when planning meals</p>
            </div>
          </div>
        </div>

        <div class="glass-morphism rounded-xl p-8 mt-8">
          <h3 class="text-xl font-bold text-white mb-6 flex items-center gap-3">
            <i class="fas fa-clock text-green-400"></i> Meal Schedule
          </h3>
          <div class="space-y-3">
            <div class="flex justify-between items-center">
              <span class="text-purple-200">Breakfast</span>
              <span class="text-white font-bold">7:00 AM - 9:00 AM</span>
            </div>
            <div class="flex justify-between items-center">
              <span class="text-purple-200">Lunch</span>
              <span class="text-white font-bold">12:00 PM - 2:00 PM</span>
            </div>
            <div class="flex justify-between items-center">
              <span class="text-purple-200">Dinner</span>
              <span class="text-white font-bold">7:00 PM - 9:00 PM</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </main>
  <%- include('../_footer') %>
</body>
</html>
